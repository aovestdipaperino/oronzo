[package]
name = "claudio"
version = "0.2.0"
edition = "2024"
description = "Search across Claude Code sessions with interactive resume"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6"

[dev-dependencies]
tempfile = "3"
