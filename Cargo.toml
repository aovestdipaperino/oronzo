[package]
name = "claudio"
version = "0.3.0"
edition = "2024"
description = "A toolkit for Claude Code sessions"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6"

[build-dependencies]
logo-art = "0.2"
image = "0.25"

[dev-dependencies]
tempfile = "3"
